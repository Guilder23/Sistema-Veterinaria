// Reportes specific functionality
document.addEventListener('DOMContentLoaded', function() {
    console.log('Módulo de Reportes cargado');
    // Chart logic is already in the HTML for simple cases, but can be moved here
});
